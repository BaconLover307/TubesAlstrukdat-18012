#include <stdio.h>
#include "load.h"

void LoadFile(Sinfotype * state, Graph * relasi, MATRIKS * map) {
    // Kamus Lokal
    char loadfile[100] = "coba.txt";
    // Algoritma
    STARTKATA(loadfile);
    // todo LoadFile
}
